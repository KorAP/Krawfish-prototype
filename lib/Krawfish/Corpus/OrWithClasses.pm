package Krawfish::Corpus::OrWithClasses;
use parent 'Krawfish::Corpus::Or';
use Krawfish::Log;
use strict;
use warnings;

# "or with classes" queries are similar
# to "or" queries, but they respect classes
# and are therefore not cachable

1;
